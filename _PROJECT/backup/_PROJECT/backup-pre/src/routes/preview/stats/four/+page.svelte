<script lang="ts">
	import { stats } from "$lib/config/blocks_main/stats";

	const block = stats.find((item) => item.title === "four");

	if (!block) {
		throw new Error("Missing preview block for four in stats");
	}

	const PreviewComponent = block.component;
</script>

<PreviewComponent />
